class Solution:
    def maximumProduct(self, nums: List[int]) -> int:
        
        nums=sorted(nums,reverse=True)
        
        x1=nums[-1]*nums[-2]*nums[0]
        
        x2=nums[0]*nums[1]*nums[2]
        
        return(max(x1,x2))
        
