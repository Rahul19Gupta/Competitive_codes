class Solution:
    def isPalindrome(self, x: int) -> bool:
        
        x=str(x)
        
        rev_x=x[::-1]
        
        if(x==rev_x):
            return(True)
        
        return(False)
        
